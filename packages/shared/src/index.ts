/**
 * @file Main Entry Point for @auth/shared
 *
 * Re-exports schemas and types so consumers can import from the package root:
 *   import { LoginSchema, JwtAccessPayload } from "@auth/shared"
 */
export * from "./schemas/index.js";
export * from "./types/index.js";
